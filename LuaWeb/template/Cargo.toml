[package]
name = "lwtemplate"
version = "1.0.0"
edition = "2021"
description = "LuaWeb Template Engine"

[lib]
name = "lwtemplate"
crate-type = ["cdylib", "staticlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
lto = true
opt-level = 3
